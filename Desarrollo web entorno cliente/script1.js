// Suma del 1 al 10
let limite = 10;
let suma = 0;
for (let i = 0; i < limite; i++) {
    suma +=i;
    }

    document.write("La suma de todos los numeros del " + "1" + " al " + limite + " es: " + suma + ".")

